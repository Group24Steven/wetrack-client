import { WeekdayDatePipe } from './weekday-date.pipe';

describe('WeekdayDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekdayDatePipe();
    expect(pipe).toBeTruthy();
  });
});

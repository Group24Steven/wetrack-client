import { Tenant } from './tenant';

describe('Tenant', () => {
  it('should create an instance', () => {
    //expect(new Tenant(1, 'test@test.de', 'TestToken', 'http://localhost.de')).toBeTruthy();
  });
});

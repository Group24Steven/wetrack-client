import { WorkPercentPipe } from './work-percent.pipe';

describe('WorkPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkPercentPipe();
    expect(pipe).toBeTruthy();
  });
});

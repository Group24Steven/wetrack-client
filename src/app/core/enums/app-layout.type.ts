export enum AppLayoutType {
  Default = 'default',
  Sidenav = 'sidenav', // with sidebar
  Center = 'center', // centered content, no frame
  Blank = 'blank', // no frame
  Admin = 'admin', // admin layout
}
